# ══════════════════════════════════════════════════════════════
# FishClaw 环境配置
# 复制此文件为 .env 并填写真实值，不要将 .env 提交到版本控制
# ══════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────
# 闲鱼页面 URL 配置
# ──────────────────────────────────────────────────────────────
XIANYU_HOME_URL=https://www.goofish.com
# ──────────────────────────────────────────────────────────────
# 智能体（Agent）用 LLM 配置
# 用于 Agno 智能体推理，与爬虫 LLM 独立配置
# ──────────────────────────────────────────────────────────────
AGENT_LLM_MODEL=qwen-max
AGENT_LLM_API_KEY=sk-xxx
AGENT_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
AGENT_LLM_TEMPERATURE=0.5

# ──────────────────────────────────────────────────────────────
# 浏览器无头模式配置
# CRAWLER_HEADLESS  : crawl4ai 探针层，只读不操作，true 即可
# PLAYWRIGHT_HEADLESS: 实际登录/操作层，建议 false（有头更难被风控）
# ──────────────────────────────────────────────────────────────
CRAWLER_HEADLESS=true
PLAYWRIGHT_HEADLESS=false

# ──────────────────────────────────────────────────────────────
# 图像生成 API Key
# ──────────────────────────────────────────────────────────────
IMAGE_API_KEY=sk-xxx

# ──────────────────────────────────────────────────────────────
# 浏览 & 评论 Agent 配置
# BROWSE_COMMENT_TEXT: 在他人帖子下自动评论的内容
# ──────────────────────────────────────────────────────────────
BROWSE_COMMENT_TEXT=其实养号很简单，就像我这样瞎溜达。既可以给楼主增加流量，又可以给自己提高曝光，其实，提升曝光度很简单，每天发言评论点赞就行了，曝光度能达到10万，前提是你要坚持七天＃，只要坚持七天，后面也要坚持成为优质用户，只要坚持七天，每天曝光度10万到20万之间。我测试过得很稳定也可以复制我这段话。去其他地方评论，记得给我点赞，祝楼主生意兴降其实提升曝光度很简单的，像我这样每天发言评论点赞就可以了，曝光度能达到10w这不假，前提是你要像我这样子才行、经常7天，你就是高权重、高价值用户，成为优质用户只需要坚持7天后面也需要坚持，每天的曝光度到10w-12W之间、我测试过很稳定的也可以复制我这段话去别的地方评论记

# ──────────────────────────────────────────────────────────────
# 提示词生成工具 system prompt（prompt_tools）
# PROMPT_IMAGE_SYSTEM    : 生图提示词的 system prompt
# PROMPT_DESCRIPTION_SYSTEM: 商品描述文案的 system prompt
# ──────────────────────────────────────────────────────────────
PROMPT_IMAGE_SYSTEM="You are a professional image prompt engineer specializing in tech-aesthetic visuals.
Generate a concise image generation prompt based on the given technology topic.

Hard rules:
- NO Chinese characters anywhere in the output
- Visual style: dark background, neon / holographic / cyberpunk, circuit patterns, glowing code
- Naturally embed 3-5 short professional English tech terms as visible text elements in the scene (e.g., labels, HUDs, floating tags) — keep each term under 3 words
- Total prompt length: 60-120 words
- Output ONLY the prompt, no explanation, no prefix"

PROMPT_DESCRIPTION_SYSTEM="你是一名在闲鱼长期接单的程序员，现在要写一段闲鱼服务商品描述。

根据给定的技术主题，严格按照以下结构和格式输出，不要修改格式骨架，注意语气要客观描述，不要迎合客户：

注意：
1. 语气要客观描述，不要迎合客户，不要迎合客户，不要迎合客户
2. 禁止使用emoji表情

【第一段，1~2行】
简短自我介绍，点明专注方向，末尾说「帮你快速脱坑」。
格式参考：本人 (互联网一线从业者)，专注 XX 方向，帮你快速脱坑，把时间花在更重要的事情上。

【第二段，用下面的分隔线括起来】
-----------------------------------------------------
我能帮你解决这些事儿 (主打一个专注):
【分类名】：用口语写，说清楚能做什么、具体怎么操作，不要泛泛而谈。
（根据主题选 3~5 个最合适的分类，常见分类：环境配置、代码调试、模型训练、模型使用、参数调优、部署上线、数据处理、效果优化等）
-----------------------------------------------------

【第三段，用下面的分隔线括起来】
-----------------------------------------------------
主攻技术 (为了让你能搜到我):
按 2~4 个维度分组，尽量多列相关的技术/框架/工具/模型名，用逗号分隔。
格式参考：AIGC 生成框架： Stable Diffusion, Flux, ComfyUI
-----------------------------------------------------

严格禁止：
- 禁止编造虚构用户场景（设计师、艺术家、老板、企业主等）
- 禁止使用效率、赋能、解决方案、激发创意、节省时间等空洞词汇
- 禁止介绍这个技术是什么或有什么意义
- 禁止加多余的标题行、前言、结束语
- 只输出正文，不加任何说明"